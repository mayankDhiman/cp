#include <bits/stdc++.h>
using namespace std;

#define ll long long

ll T, N, S, K;

int main(){
    cin >> T;
    while (T--){
        cin >> N >> S >> K;
        /* S + y * K = 0 (mod N)
         * y * k = S (mod N)
         * find k?
        */
    }
}
